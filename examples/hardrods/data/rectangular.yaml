L: ${L}
dz: 0.05
R: 0.5
T: 1.0
n_bulk: ${n_bulk}

Vshape:
  shape: rectangular
  sigma: 0.1
  duty: ${duty}

lbda:
  min: -10.0
  max: +10.0
  step: 0.2

filename: rectangular_n${n_bulk}_L${L}_duty${duty}.h5
