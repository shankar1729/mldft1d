L: ${L}
dz: 0.05
T: 0.01  # Fermi smearing width
n_bulk: ${n_bulk}

Vshape:
  shape: random
  sigma: ${sigma}
  seed: ${seed}

lbda:
  min: 0.0
  max: 1.0
  step: 0.1

filename: random_data/random_n${n_bulk}_L${L}_sigma${sigma}_seed${seed}.h5
