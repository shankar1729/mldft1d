L: ${L}
dz: 0.05
R: 0.5
T: 1.0
n_bulk: ${n_bulk}

Vshape:
  shape: random
  sigma: ${sigma}
  seed: ${seed}

lbda:
  min: 0.0
  max: 5.0
  step: 0.5

filename: random_data/random_n${n_bulk}_L${L}_sigma${sigma}_seed${seed}.h5
